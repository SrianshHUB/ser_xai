# FILE: requirements.txt
# DESCRIPTION: List of Python dependencies for the backend.
# CONTRIBUTION: Ensures that the Flask server, audio processing (librosa), and ML libraries (scikit-learn, shap, joblib) are consistent across environments.
flask>=2.0.0
librosa>=0.9.0
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0
joblib>=1.0.0
soundfile>=0.10.0
shap>=0.40.0
pydub>=0.25.0  # For audio format conversion (optional but recommended)